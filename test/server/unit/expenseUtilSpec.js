const expect   = require('chai').expect;
      util     = require('../../../server/util/expenseUtil');
      // csv      = require('csv'),
      // uncomment when you have placed a test.csv file in server route
      // testCSV  = __dirname + '../../test.csv';

describe('parsing different bank data', () => {
  it('should properly add Bank of America expenses to DB', () => {
    
  });
});
